## (1) Starting the Back End Server
Navigate to the back end directory and type the command,
"go run main.go", the server should initialize. The server is viewable at localhost:8080.

## (2) Starting the Front End Server
Navigate to the front end directory and type the command,
"bun run dev", this should link you to the localhost:3000.
